--(localdb)\MSSQLLocalDb
DROP TABLE MovieDatabase.WatchList;

IF OBJECT_ID(N'MovieDatabase.WatchList') IS NULL
BEGIN
	CREATE TABLE MovieDatabase.WatchList
	(
		watchListId int IDENTITY(1,1) PRIMARY KEY,
		movieId INT --NOT NULL??????????????????????????????????????????????????????????????
			FOREIGN KEY REFERENCES MovieDatabase.Movies(movieId),
		accountId INT --NOT NULL??????????????????????????????????????????????????????????????
			FOREIGN KEY REFERENCES MovieDatabase.Account(accountId)
	);
END;

--create the relationship table for Languages

SELECT *
FROM MovieDatabase.WatchList

